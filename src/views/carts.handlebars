{{#each carts}}
    <p>id del carrito : {{this._id}}</p>
    {{#each products}}
        <p>id del producto : {{this._id}}</p>
        <p>Cantidad: {{this.quantity}}</p>
    {{/each}}
     </br>
{{/each}}
